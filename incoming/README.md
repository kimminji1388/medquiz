# Incoming Quiz HTML

Upload new quiz HTML files to this folder on GitHub.

The `Import quiz HTML` workflow converts supported files, validates the result,
updates quiz data and images, and moves processed HTML files to `archive/html`.

Use the same filename to replace a previously imported question set. Use a new
filename to create a new set.
